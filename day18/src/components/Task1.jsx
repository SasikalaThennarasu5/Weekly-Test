// src/components/Task1.jsx
import React from 'react';

const Task1 = () => {
  return (
    <div>
      <h2>What is Axios?</h2>
      <p>
        Axios is a promise-based HTTP client used in JavaScript and React to send requests to APIs and handle responses.
        It simplifies the process of making HTTP calls and has advantages over fetch() like interceptors, automatic JSON handling, and cancellation support.
      </p>
    </div>
  );
};

export default Task1;
